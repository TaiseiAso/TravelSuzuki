#ifndef scene_mover_h
#define scene_mover_h

class SceneMover
{/*
private:
	static SceneMover* instance; // シングルトンインスタンス
	
	// コンストラクタ
	SceneMover();
	// デストラクタ
	~SceneMover();

public:
	// コピーコンストラクタ
	SceneMover(const SceneMover&) = delete;
	SceneMover(SceneMover&&) = delete;
	// コピー代入演算子
	SceneMover& operator=(const SceneMover&) = delete;
	SceneMover& operator=(SceneMover&&) = delete;

	// シングルトンインスタンスを取得する
	static SceneMover& GetInstance();
	// シングルトンインスタンスを生成する
	static void Create();
	// シングルトンインスタンスを破棄する
	static void Destroy();*/
};

//SceneMover* SceneMover::instance = nullptr;

#endif // !scene_mover_h